<Window x:Class="BetterAccounting.UI.Views.BackupRestoreView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup & Restore" Height="400" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Commands -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Create Backup" Command="{Binding BackupCommand}" Margin="5"/>
            <Button Content="Refresh List" Command="{Binding RefreshCommand}" Margin="5"/>
        </StackPanel>

        <!-- Available Backups -->
        <Label Grid.Row="1" Content="Available Backups:" Margin="0,0,0,5"/>
        <ListBox Grid.Row="1" ItemsSource="{Binding Backups}" 
                 SelectedItem="{Binding SelectedBackup}" 
                 Margin="0,5,0,0" Height="150">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Manual Restore -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="Or select file:" VerticalAlignment="Center"/>
            <Button Content="Browse" Command="{Binding BrowseCommand}" Margin="10,0"/>
        </StackPanel>

        <!-- Restore Button -->
        <Button Grid.Row="3" Content="Restore Selected Backup" 
                Command="{Binding RestoreCommand}" 
                Margin="0,10,0,0" Height="30"/>

        <!-- Status -->
        <StatusBar Grid.Row="3" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
