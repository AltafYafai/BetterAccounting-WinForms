<Window x:Class="BetterAccounting.UI.Views.GstSlabView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GST Slabs" Height="520" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Name:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding SlabName, UpdateSourceTrigger=PropertyChanged}" Width="140" VerticalAlignment="Center"/>
            <Label Content="Rate %:" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox Text="{Binding SlabRate, UpdateSourceTrigger=PropertyChanged}" Width="80" VerticalAlignment="Center"/>
            <Button Content="Add" Command="{Binding AddCommand}" Padding="10,2" Margin="10,0,0,0"/>
            <Button Content="Delete Selected" Command="{Binding DeleteCommand}" Padding="10,2" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Slabs}" SelectedItem="{Binding SelectedSlab}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Rate %" Binding="{Binding Rate}" Width="120"/>
                <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>