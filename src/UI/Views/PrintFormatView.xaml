<Window x:Class="BetterAccounting.UI.Views.PrintFormatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Print Formats" Height="660" Width="1020">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Document Type:" VerticalAlignment="Center"/>
            <ComboBox Width="110" ItemsSource="{Binding DocumentTypes}" SelectedItem="{Binding SelectedDocumentType}" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <Label Content="Format Name:" VerticalAlignment="Center"/>
            <TextBox Width="160" Text="{Binding TemplateName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="New" Command="{Binding NewCommand}" Padding="10,3" Margin="0,0,5,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="10,3" Margin="0,0,5,0"/>
            <Button Content="Set Default" Command="{Binding SetDefaultCommand}" Padding="10,3" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="10,3"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Template list -->
            <DockPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock DockPanel.Dock="Top" Text="Saved Formats" FontWeight="Bold" Margin="0,0,0,6"/>
                <ListBox ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}"
                         DisplayMemberPath="Name"/>
            </DockPanel>

            <!-- Editor + preview -->
            <TabControl Grid.Column="1">
                <TabItem Header="Editor">
                    <DockPanel Margin="6">
                        <TextBlock DockPanel.Dock="Top" Text="Insert a field by clicking a token below, then edit the layout text." 
                                   Foreground="Gray" Margin="0,0,0,6"/>
                        <ItemsControl DockPanel.Dock="Top" ItemsSource="{Binding AvailableTokens}" Margin="0,0,0,8">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" Margin="2" Padding="7,2"
                                            Command="{Binding DataContext.InsertTokenCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBox Text="{Binding TemplateContent, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" AcceptsTab="True" 
                                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="13" TextWrapping="NoWrap"/>
                    </DockPanel>
                </TabItem>
                <TabItem Header="Preview">
                    <FlowDocumentReader Document="{Binding PreviewDocument}"/>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Status -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>